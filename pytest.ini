[pytest]

testpaths = tests

addopts = --ignore=tests/draft -v

python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    smoke: 冒烟测试用例，覆盖核心主流程
    regression: 回归测试用例，覆盖全量业务场景
    debug: 调试专用用例，正式执行默认排除