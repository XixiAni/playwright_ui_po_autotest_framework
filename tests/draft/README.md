# 草稿用例目录
本目录用于存放**调试脚本、临时验证用例、学习测试代码**，不会被正式执行的用例集纳入。

## 规则说明
1. 正式执行 `pytest` 时，pytest.ini 配置会自动忽略本目录
2. 如需执行草稿目录用例，显式指定路径即可：`pytest tests/draft/xxx.py -vs`
3. 调试完成的正式用例，请移动到 `tests/` 根目录或对应业务子目录
4. 本目录内文件不做强制规范，可自由编写调试代码
